module.exports = {
  plugins: [
    `gatsby-transformer-asciidoc`,
    {
      resolve: `gatsby-source-filesystem`,
      options: {
        name: `pages`,
        path: `${__dirname}/src/pages/`,
      },
    },
  ],
}
